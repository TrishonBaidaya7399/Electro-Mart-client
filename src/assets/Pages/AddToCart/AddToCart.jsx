// import PropTypes from 'prop-types';

const AddToCart = () => {
    return (
        <div>
            <h1>Added to cart</h1>
        </div>
    );
};

AddToCart.propTypes = {
    
};

export default AddToCart;